// 接口地址配置。当前为纯前端演示版，正式接入云开发/服务器 API 时只改这里。
const API_CONFIG = {
  baseURL: "",
  endpoints: {
    login: "/api/auth/login",
    dashboard: "/api/dashboard",
    orders: "/api/orders",
    shops: "/api/shops",
    staff: "/api/staff",
    products: "/api/products",
    logs: "/api/logs"
  }
};