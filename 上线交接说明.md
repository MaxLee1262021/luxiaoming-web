# 鹿小鸣管理后台 · 上线交接说明

> 给技术部署人员的快速上手 + 关键约束说明。请按顺序读、做。

## 一、这是什么

鹿小鸣旅拍微信小程序的配套 Web 管理后台。当前为**演示版 + 自托管双模式**：

- **前端**：原生 HTML + Vue 3（全局构建，no-build 模式，无 HMR），Element Plus 跑在本地 vendor 包里
- **后端**：`server/index.cjs` 双模式骨架，**默认走 mock 演示数据**，配齐环境变量后切到云开发真实库
- **数据**：`server/index.cjs` 走 json 模式时，业务数据落 `server/data/db.json`（**未进交付包**，首次启动由 `server/seed.cjs` 种子生成）

## 二、目录结构（请以本说明为准，README 描述略旧）

```text
luxiaoming-admin-交付包/
├─ index.html                # 应用入口（按序加载 public/vendor + src/app/* + src/views/*）
├─ package.json              # 脚本与依赖（依赖只有 @cloudbase/node-sdk + dotenv）
├─ package-lock.json
├─ .env.example              # 环境变量模板（部署时复制成 .env 再填值）
├─ public/                   # 第三方 vendor 资源（vue / element-plus / 全套）
├─ src/
│  ├─ app/                   # ★ Vue 应用拆分的 17 个业务模块（01-core-state ... 17-misc-pager）
│  ├─ views/                 # 30 个业务页面（每个含 js / html / css 三件套）
│  ├─ router/                # 路由 + 页面动态注册（必须保证 30 页 × 3 文件齐全）
│  ├─ layout/                # 侧边栏 + 顶栏壳层
│  ├─ api/                   # 接口封装（business-service / cloud / lxmApi 三个活文件）
│  ├─ config/                # 业务配置 / 角色权限 / 状态枚举
│  ├─ mock/                  # 演示数据
│  ├─ styles/                # 全局样式（含 glass-theme.css，主题最后加载）
│  ├─ utils/
│  └─ assets/
├─ server/                   # 后端双模式服务
│  ├─ index.cjs              # 入口（mock / cloud 双模式按 env 切）
│  ├─ seed.cjs               # 首次启动种子数据
│  └─ lib/                   # api / dbSource / cloudSource
├─ scripts/
│  └─ serve.cjs              # 静态前端服务（npm run dev 用这个）
├─ docs/                     # 设计文档（仅供理解，不参与运行）
├─ .gitignore
├─ .editorconfig
├─ .prettierrc
├─ eslint.config.js
├─ jsconfig.json
├─ vite.config.js
└─ 视觉统一层说明.md         # 主题/视觉令牌说明
```

> ⚠️ README.md 里描述的 `src/app.js` 是历史结构，实际入口已拆分到 `src/app/01-core-state.js` 等 17 个模块文件，由 `window.LXM_APP_PARTS` 按序注册。**index.html 加载顺序必须保持现状**。

## 三、启动方式（两套，二选一）

### A. 静态前端（仅前端演示，**不连真实数据**）

适合只验证 UI 能不能跑起来：

```bash
cd luxiaoming-admin-交付包
npm install        # 安装 @cloudbase/node-sdk + dotenv
npm run dev        # → node scripts/serve.cjs
# 浏览器打开 http://127.0.0.1:5191/
# 演示账号 admin / lxm2026
```

> 静态前端模式下，前端请求会被前端 mock 拦截，**不会真正打到后端**。这是设计如此，不是 bug。

### B. 完整后端（json 模式 / cloud 模式）

```bash
cd luxiaoming-admin-交付包
npm install

# 1) 先把 .env.example 复制成 .env，按需修改
cp .env.example .env
# 用编辑器打开 .env，至少设置：
#   CLOUDBASE_ENV_ID=你的云环境 ID
#   CLOUDBASE_SECRET_ID=你的服务端 API Key ID
#   CLOUDBASE_SECRET_KEY=你的服务端 API Key
#   ADMIN_PASSWORD=你给运维设置的强密码（默认 lxm2026）
#
# 不填云密钥 → 自动走 mock 演示数据
# 填了云密钥 → 自动走 cloud 模式读写真实云数据库

# 2) 启动后端（同时托管前端静态资源，端口 5191）
npm run server
# 浏览器打开 http://127.0.0.1:5191/
```

## 四、环境变量（.env 必读）

| 字段 | 必填 | 说明 |
|---|---|---|
| `CLOUDBASE_ENV_ID` | cloud 模式必填 | 腾讯云开发环境 ID |
| `CLOUDBASE_SECRET_ID` | cloud 模式必填 | 云 API 密钥 ID（建议用**服务端** API Key，别用前端 Key） |
| `CLOUDBASE_SECRET_KEY` | cloud 模式必填 | 云 API 密钥内容 |
| `ADMIN_PASSWORD` | 建议必填 | 登录密码；不填则走代码里 hardcode 的 `lxm2026`（仅供本地，**生产必须改**） |

> **绝对不要把 .env 提交到 git**（已在 .gitignore 里）。交付包里的 `.env.example` 只是模板。

## 五、当前已实现功能

- 30 个业务页面：经营看板、订单/售后/财务、对账、人员/分销/商家、内容总览/小程序首页装修/旅拍内容（照片/视频/套餐/周边/灵感）/素材库/打卡点/城市/拍摄风格、上下架/审核/操作日志/回收站等
- 角色权限：系统超管 / 客服 / 财务 / 摄影师 / 分销员 / 商家 / 内容运营
- 数据持久化：cloud 模式直接读写云开发数据库；json 模式落本地 db.json（**未进交付包**）

## 六、当前边界 & 上线前必须做的事

1. **改 ADMIN_PASSWORD**：`.env` 里改成强密码，scrypt 哈希存储（`lxm1$<salt>$<hash>` 格式）
2. **配云环境**：在腾讯云开发创建环境，把 `CLOUDBASE_*` 三个值填进 `.env`
3. **数据迁移**：seed.cjs 里的演示数据需要替换为真实业务数据
4. **HTTPS + 域名**：生产部署要套域名 + SSL，前端静态资源走 CDN
5. **进程守护**：建议用 PM2 / systemd 拉起 `node server/index.cjs`

## 七、常见坑

- ❗ **不要同时跑 `npm run dev` 和 `npm run server`**，会共用 db.json 互相覆盖。生产只用 server，开发各自跑 dev
- ❗ 改了 `server/lib/*.cjs` 必须**重启进程**才生效（没有 HMR）
- ❗ 改了 `.env` 必须重启进程
- ❗ 缺任何一个 `src/views/{folder}/{slug}.{js,html,css}` 三件套文件，对应菜单会**白屏**（no-build 模式不抛错只静默失败）
- ❗ `index.html` 里 script 标签的加载顺序：vendor → app modules → router → 各 views。**不要改顺序**
- ❗ `samples` 集合是孤儿数据，建议运维从数据迁移里剔除

## 八、给技术的备注

- 整个项目**无构建步骤**（不跑 webpack / vite），交付即用
- 所有 30 个页面按需懒加载，router 里 register → `loadScript` → 注入 DOM
- 主要安全机制：scrypt 哈希（`lxm1$<salt>$<hash>`）、登录审计日志、staff 停用 / 商家暂停合作会在登录时 403 拒绝
- 内部简称：鹿小鸣 = `lxm`；后台所有命名遵循这个前缀

---

**联系方式**：部署过程中遇到项目本身的设计/逻辑疑问，回找项目负责人核对，不要自己改业务规则。
