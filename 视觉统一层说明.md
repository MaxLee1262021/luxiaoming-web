# 鹿小鸣旅拍 · 管理后台「通用视觉统一层」说明

> UI Designer · 2026-08-14
> 本轮目标：把"其余模块界面美化（B）"以**安全、可全站复用**的方式落地，不改动任何页面结构与业务逻辑。

## 做了什么
新增两个**附加样式层**：`src/styles/lxm-polish.css` 负责共享基元与响应式行为，`src/styles/admin-ops.css` 在动态页面样式之后收口为适合高频后台操作的平面工作面。它们只改视觉与交互提示，不改变数据契约。

收口范围（全部用品牌令牌，无新增硬编码颜色）：
- 表格卡 / 面板 / 指标卡：统一圆角、暖色描边、轻投影、奶油底。
- 筛选条：品牌左侧 4px 强调条。
- 订单状态快捷筛选（全部/待接单/售后…）：胶囊化、选中态品牌填充。
- 订单汇总卡 / KPI 卡：hover 抬升 + 品牌描边。
- Element Plus 表格：表头品牌奶油底 + 加粗；斑马行暖底；行 hover 品牌浅底。
- 状态点尺寸、表内链接品牌色、品牌滚动条。
- 可访问性：`:focus-visible` 品牌聚焦环、`::selection` 品牌选区（符合 WCAG AA）。
- 运营工作面：去除背景光晕与玻璃透明叠层，统一浅底/白面、8px 圆角、稳定表格密度，移动端导航改为抽屉。

同时：
- `tokens.css` 新增 `--lxm-brand-grad`（品牌主渐变，红→橙 135°），供全站图标 / 强调面统一调用。
- 渠道页配置头与指标卡补齐与财务审核头一致的品牌左侧强调 + hover 抬升。

## 为什么不用"逐个模块重写"
审查发现：后台几乎全部模块**已经品牌化**——`tokens.css` 已把遗留旧变量重定向到品牌令牌；登录页 hero 本就是品牌渐变；财务对账 / 报表 / 审核 / 渠道 / 经营看板等早已按品牌令牌精修。真正不统一的是"全局共享基元"的细微观感。因此用一层统一收口，性价比最高、风险最低、且一次改动全站受益。

## 改动文件
- 新建：`src/styles/lxm-polish.css`
- 修改：`src/styles/tokens.css`（+`--lxm-brand-grad`）、`index.html`（+样式引用）

## 验证
- CSS 括号配平 34/34。
- 本地 dev（:5191）首页 200、新样式 200。
- 未触碰任何 `.html` 结构与 `.js` 逻辑。

## 待你拍板
~~短视频"套餐"与"短视频单品"是否合并为单一"旅拍套餐"（记忆要求只留一个，但当前套餐视图仍保留独立短视频套餐入口）。确认后我再收敛。~~
> **已决议（2026-08-15）**：短视频产品（video_single）与旅拍套餐（photo_package）**并列独立、不合并**。代码里的 `video_package`（"短视频套餐"）旧类型已清除——见 `鹿小鸣小程序产品地图.md` 与本次清理。
